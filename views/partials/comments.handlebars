<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.username}}
            {{!-- need to work on adding date --}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>